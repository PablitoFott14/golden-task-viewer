node_modules
dist
dist-ssr
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json

# Claude / agent tooling
CLAUDE.md
.claude/

# Heavy raw source-of-truth artifacts (not loaded at runtime).
# Trajectories live in each task's root folder (sibling of viewer/), not here.
public/model's behave/
public/tasks/**/trajectory.json
